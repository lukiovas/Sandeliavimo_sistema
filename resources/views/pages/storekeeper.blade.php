@extends('layouts.pageLayout')

<?php $logged = true ?>
@section('content')
<h2>Storekeeper panel</h2>
{{-- @if(count($panel) > 0)
    <ul class="list-group">
        @foreach($panel as $option)
    <li class="list-group-item">{{$option}}</li>
        @endforeach
    </ul>
@endif --}}

@endsection